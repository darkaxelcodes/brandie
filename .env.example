# Supabase Configuration (required)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Mixpanel Analytics (optional)
VITE_MIXPANEL_TOKEN=your_mixpanel_project_token

# Note: OpenAI and ElevenLabs API keys are configured in Supabase Edge Functions
# These are NOT exposed to the browser and should be set as secrets in your Supabase project:
# - OPENAI_API_KEY (required for AI features)
# - ELEVENLABS_API_KEY (required for voice features)
# - ELEVENLABS_VOICE_ID (optional, defaults to 'pNInz6obpgDQGcFmaJgB')